{
  "name": "cli-iprice-test",
  "version": "1.0.0",
  "description": "CLI to convert charaters to uppercase and lowercase and generates CSV",
  "main": "./cli.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "sc chia",
  "license": "ISC",
  "bin": {
    "string2csv": "./cli.js"
  },
  "dependencies": {
    "csv-writer": "^1.6.0",
    "inquirer": "^7.1.0"
  }
}
